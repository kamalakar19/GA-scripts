name: falseoption concurrency demo 

on: workflow_dispatch

concurrency:
  group: main-branch-group
  cancel-in-progress: false

jobs:
  demo:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Running workflow"
      - run: sleep 300
      - run: echo "Finished"
