
name: matrix example 3

on: workflow_dispatch

jobs:
  test:
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [16, 18]
    steps:
     - name: Show values
       run: |
         echo "os=${{matrix.os}}"
         echo "node=${{matix.node}}"