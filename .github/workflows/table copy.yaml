name: to print table

on: workflow_dispatch

jobs:
    table:
        runs-on: ubuntu-latest
        steps:
          - name: checkout code
            uses: actions/checkout@v7
          - name: multiple
            run: |
              cat tab.sh
              cat-n tab.sh
              uptime
      
              sleep 20
      
              chmod u+x tab.sh
              ./tab.sh